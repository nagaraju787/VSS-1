import { DigitsDirective } from './digits.directive';

describe('DigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitsDirective();
    expect(directive).toBeTruthy();
  });
});
